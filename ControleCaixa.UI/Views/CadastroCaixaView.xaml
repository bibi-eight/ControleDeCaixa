<Window x:Class="ControleCaixa.UI.Views.CadastroCaixaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Titulo}"
        Width="430"
        Height="340"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#F3F4F6">

    <Grid Margin="30">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock
            Text="{Binding Titulo}"
            FontSize="26"
            FontWeight="Bold"
            Foreground="#111827"/>

        <StackPanel
            Grid.Row="1"
            Margin="0,20,0,0">

            <TextBlock
                Text="Nome"
                FontWeight="SemiBold"
                Foreground="#374151"/>

            <TextBox
                Height="38"
                Margin="0,7,0,0"
                Padding="10,6"
                Text="{Binding Nome,
                               UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel
            Grid.Row="2"
            Margin="0,18,0,0">

            <TextBlock
                Text="Saldo mínimo"
                FontWeight="SemiBold"
                Foreground="#374151"/>

            <TextBox
                Height="38"
                Margin="0,7,0,0"
                Padding="10,6"
                Text="{Binding SaldoMinimo,
                               UpdateSourceTrigger=LostFocus}"/>
        </StackPanel>

        <TextBlock
            Grid.Row="3"
            Margin="0,10,0,0"
            Text="{Binding Mensagem}"
            Foreground="#DC2626"
            TextWrapping="Wrap"/>

        <StackPanel
            Grid.Row="4"
            Margin="0,10,0,0"
            Orientation="Horizontal"
            HorizontalAlignment="Right">

            <Button
                Width="100"
                Height="38"
                Margin="0,0,10,0"
                Content="Cancelar"
                Command="{Binding CancelarCommand}"/>

            <Button
                Width="150"
                Height="38"
                Content="{Binding TextoBotao}"
                Background="#16A34A"
                Foreground="White"
                BorderThickness="0"
                FontWeight="SemiBold"
                Command="{Binding SalvarCommand}"/>

        </StackPanel>

    </Grid>
</Window>