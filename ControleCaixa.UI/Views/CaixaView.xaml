<Window x:Class="ControleCaixa.UI.Views.CaixaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Controle de Caixa"
        Width="850"
        Height="600"
        MinWidth="650"
        MinHeight="450"
        WindowStartupLocation="CenterScreen"
        Background="#F3F4F6">

    <Window.Resources>

        <!-- Botão verde de cadastro -->
        <Style x:Key="CadastrarButtonStyle"
               TargetType="Button">

            <Setter Property="Background"
                    Value="#16A34A"/>

            <Setter Property="Foreground"
                    Value="White"/>

            <Setter Property="BorderThickness"
                    Value="0"/>

            <Setter Property="Padding"
                    Value="18,10"/>

            <Setter Property="FontSize"
                    Value="14"/>

            <Setter Property="FontWeight"
                    Value="SemiBold"/>

            <Setter Property="Cursor"
                    Value="Hand"/>

            <Setter Property="Template">
                <Setter.Value>

                    <ControlTemplate TargetType="Button">

                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">

                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>

                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver"
                                     Value="True">

                                <Setter TargetName="ButtonBorder"
                                        Property="Background"
                                        Value="#15803D"/>

                            </Trigger>

                            <Trigger Property="IsPressed"
                                     Value="True">

                                <Setter TargetName="ButtonBorder"
                                        Property="Background"
                                        Value="#166534"/>

                            </Trigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>

                </Setter.Value>
            </Setter>

        </Style>

        <!-- Estilo de cada caixa -->
        <Style x:Key="CaixaCardStyle"
               TargetType="Button">

            <Setter Property="Background"
                    Value="White"/>

            <Setter Property="BorderBrush"
                    Value="#E5E7EB"/>

            <Setter Property="BorderThickness"
                    Value="1"/>

            <Setter Property="HorizontalContentAlignment"
                    Value="Stretch"/>

            <Setter Property="Padding"
                    Value="20"/>

            <Setter Property="Margin"
                    Value="0,0,0,12"/>

            <Setter Property="Cursor"
                    Value="Hand"/>

            <Setter Property="Template">
                <Setter.Value>

                    <ControlTemplate TargetType="Button">

                        <Border x:Name="CardBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">

                            <ContentPresenter
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>

                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver"
                                     Value="True">

                                <Setter TargetName="CardBorder"
                                        Property="Background"
                                        Value="#F9FAFB"/>

                                <Setter TargetName="CardBorder"
                                        Property="BorderBrush"
                                        Value="#16A34A"/>

                            </Trigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>

                </Setter.Value>
            </Setter>

        </Style>

    </Window.Resources>

    <Grid Margin="30">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Cabeçalho -->
        <Grid Grid.Row="0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">

                <TextBlock
                    Text="Meus caixas"
                    FontSize="28"
                    FontWeight="Bold"
                    Foreground="#111827"/>

                <TextBlock
                    Margin="0,5,0,0"
                    Text="Selecione um caixa para visualizar suas movimentações."
                    FontSize="14"
                    Foreground="#6B7280"/>

            </StackPanel>

            <Button
                Grid.Column="1"
                Content="+ Cadastrar caixa"
                Command="{Binding AbrirCadastroCommand}"
                Style="{StaticResource CadastrarButtonStyle}"/>

        </Grid>

        <!-- Separador -->
        <Border
            Grid.Row="1"
            Height="1"
            Margin="0,25,0,20"
            Background="#D1D5DB"/>

        <!-- Lista de caixas -->
        <ScrollViewer
            Grid.Row="2"
            VerticalScrollBarVisibility="Auto">

            <ItemsControl ItemsSource="{Binding Caixas}">

                <ItemsControl.ItemTemplate>

                    <DataTemplate>

                        <Button
                            Style="{StaticResource CaixaCardStyle}"
                            Command="{Binding DataContext.AbrirCaixaCommand,
                                      RelativeSource={RelativeSource AncestorType=Window}}"
                            CommandParameter="{Binding}">

                            <Grid>

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">

                                    <TextBlock
                                        Text="{Binding Nome}"
                                        FontSize="18"
                                        FontWeight="SemiBold"
                                        Foreground="#111827"/>

                                    <TextBlock
                                        Margin="0,7,0,0"
                                        Text="Clique para visualizar movimentações"
                                        FontSize="13"
                                        Foreground="#6B7280"/>

                                </StackPanel>

                                <StackPanel
                                    Grid.Column="1"
                                    Margin="20,0,0,0"
                                    VerticalAlignment="Center">

                                    <TextBlock
                                        Text="Saldo mínimo"
                                        HorizontalAlignment="Right"
                                        FontSize="12"
                                        Foreground="#6B7280"/>

                                    <TextBlock
                                        Margin="0,4,0,0"
                                        Text="{Binding SaldoMinimo,
                                                       StringFormat={}{0:C2}}"
                                        HorizontalAlignment="Right"
                                        FontSize="17"
                                        FontWeight="SemiBold"
                                        Foreground="#15803D"/>

                                </StackPanel>

                            </Grid>

                        </Button>

                    </DataTemplate>

                </ItemsControl.ItemTemplate>

            </ItemsControl>

        </ScrollViewer>

        <!-- Mensagem -->
        <TextBlock
            Grid.Row="3"
            Margin="0,15,0,0"
            Text="{Binding Mensagem}"
            TextWrapping="Wrap"
            FontSize="14"
            Foreground="#4B5563"/>

    </Grid>

</Window>